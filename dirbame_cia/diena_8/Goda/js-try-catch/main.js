console.log(   "Labas"  );

// try {
   // } catch (error) {
   
var a = 100;
try {
    alert ("value of variable a is :" + a);
 } 
 
 catch ( e ) {
    alert ("Error :" + e.desctiption);
 }
 
 [ finally {
    // Code that is always executed regardless of 
    // an exception occurring
 }]